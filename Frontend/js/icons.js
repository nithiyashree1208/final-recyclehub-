// icons.js – placeholder for inline icon rendering if needed in the future
// Currently using Unicode / emoji icons instead of SVG library
// This keeps the site fast and dependency-free

window.ReWorthIcons = {
  render: function() {
    // If we add SVG icon rendering later, it would go here
  }
};
